import { } from "./actions";

let initialstate = {

};

let reducer = (state = initialstate, action) => {
    switch (action.type) {


        default:
            return { ...state }

    }
}

export default reducer;